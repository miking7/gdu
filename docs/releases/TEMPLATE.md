<!-- Copy to docs/releases/<tag>.md (e.g. v2026.7.0.md) and fill in. The release
workflow passes this file to goreleaser via --release-notes; the GitHub release
body is exactly this content. Delete the comments. Most of it falls out of the
matching decision-log entry in docs/UPSTREAM.md. -->

## What's new in this fork release

<!-- Feature-level delta since the previous fork release: the things a user of
the previous release would notice. Not a commit list. -->

-

## Now based on dundee/gdu <!-- upstream version / PR number / short SHA -->

<!-- One line per upstream change worth calling out to fork users, then the
link. Note anything the fork deliberately adapted (with a pointer to FORK.md
or configuration.md rather than re-explaining here). -->

- Full upstream changes: https://github.com/dundee/gdu/releases

## Compatibility notes

<!-- Config keys added/renamed, snapshot/archive format notes, behavior changes
an existing user should read before upgrading. "None." is a fine answer. -->

None.
